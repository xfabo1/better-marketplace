export interface Listing {
  id: number;
  title: string;
  country: string;
  postalCode: string;
  placeName: string;
  price: number;
  currency: string;
  category: string;
  condition: string;
}
